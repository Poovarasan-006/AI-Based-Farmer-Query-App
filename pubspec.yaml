name: ai_based_farmer_query_app

description: A Flutter application integrating RAG, voice search, image search, and AI capabilities.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  rag:
  voice_search:
  image_search:
  ai_integration:

flutter:
  uses-material-design: true