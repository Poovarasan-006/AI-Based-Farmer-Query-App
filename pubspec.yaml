name: ai_based_farm_query_app

description: A new Flutter project.

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.3
  provider: ^5.0.0
  flutter_svg: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2
  build_web_compilers: ^2.13.0

flutter:
  uses-material-design: true

  assets:
    - images/  # Add your asset folder or files here
